name: test
on:
  pull_request:
    branches:
      - test/workflow

jobs:
  pr_description:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Echo
        run: |
          ls -la
          echo ${{ secrets.TEST_SECRET }}
